<h1>Hi {{ person.name }}</h1>

{% if liked %}
<form method="post" action="/person/242/unlike">
    <button type="submit" name="message" value="">Unlike 💔</button>
</form>
{% else %}
<form method="post" action="/person/242/like">
    <button type="submit">Like ❤️</button>
</form>
{% endif %}